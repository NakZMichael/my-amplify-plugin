{
  "name": "my-amplify-plugin",
  "version": "1.0.0",
  "description": "A plugin that make amplify push to create dynamoDB tables one to one instead of parallel to avoid simultaneous table creation limits exceeds.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "files": [
    "dist",
    "amplify-plugin.json"
  ],
  "author": "Muka Nakazato",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.9.1",
    "typescript": "^4.4.2"
  }
}
